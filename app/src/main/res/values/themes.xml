<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme used for the very first frame before Compose renders.
         The real look-and-feel is defined in Kotlin (ui/theme). -->
    <style name="Theme.RiddleAlarm" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
